import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders portfolio app', () => {
  // Update the text below to match actual content rendered by <App />
});
